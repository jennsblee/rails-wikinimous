
<ul class="list-group list-group-flush">
  <% @articles.each do |article| %>
    <li class="my-3 list-group-item"><%= link_to article.title, article_path(article) %><br><%= article.content %></li>
  <% end %>
</ul>
